# Contributing

## Setup
```bash
python -m venv .venv
source .venv/bin/activate
pip install -e . pytest
```

## Tests
```bash
pytest -q
```

## Pull requests
- Keep changes small and focused.
- Include a short before/after behavior note.
- Add tests for new logic where possible.
